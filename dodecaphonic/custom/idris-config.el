;;; package --- idris-config.el - Haskell coding config
;;; Commentary:
;;; Code:

(use-package idris-mode
  :config
  (use-package helm-idris))

(provide 'idris-config)

;;; idris-config.el ends here
